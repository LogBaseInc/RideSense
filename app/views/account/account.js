define(['angular',
    'config.route'], function (angular, configroute) {
    (function () {

        configroute.register.controller('account', [account]);
        function account() {
            
        }
    })();
});